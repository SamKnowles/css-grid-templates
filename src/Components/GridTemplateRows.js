import React from 'react'

function GridTemplateRows() {
    return (
        <div> The grid-template-rows property defines the height of each row. Use the grid-template-rows property to specify the size (height) of each row. Use the grid-template-rows property to specify the size (height) of each row.
            <div class="grid-container12">
                <div>1</div>
                <div>2</div>
                <div>3</div>
                <div>4</div>
                <div>5</div>
                <div>6</div>
            </div>
        </div>
    )
}

export default GridTemplateRows
